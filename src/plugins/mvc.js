//MVC system based of Aura

//Model Objects model Api
//View Objects manage HTML content and DOM actions
//Controller Objects cannot access DOM or Apis directly! - however, controller actions can be triggered from DOM or API

//implement some way of passing data between 
(function(){
    var mvc = {
        
    }
    
    //
    $$($$).addProperty({value:mvc})
})()